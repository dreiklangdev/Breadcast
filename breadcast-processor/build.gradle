apply plugin: 'java-library'

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.squareup:javapoet:1.9.0'
    compile project(path: ':breadcast-annotation')
}

sourceCompatibility = "1.8"
targetCompatibility = "1.8"
